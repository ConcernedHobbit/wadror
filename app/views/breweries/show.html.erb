<p style="color: green"><%= notice %></p>

<%= render @brewery %>

<div>
  <% if current_user %>
    <%= link_to "Edit this brewery", edit_brewery_path(@brewery) %> |
  <% end %>
  <%= link_to "Back to breweries", breweries_path %>

  <%= button_to("Destroy this brewery", @brewery, method: :delete) if current_user %>
</div>
